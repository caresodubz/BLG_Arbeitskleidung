<UserControl
    x:Class="BLG_Arbeitskleidung.Views.UserControls.BestandAnzeigenControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:db="clr-namespace:Arbeitsbekleidung.Database.Database;assembly=Arbeitsbekleidung.Database"
    xmlns:local="clr-namespace:BLG_Arbeitskleidung.Views.UserControls"
    xmlns:main="clr-namespace:BLG_Arbeitskleidung.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:BLG_Arbeitskleidung.ViewModels"
    xmlns:models="clr-namespace:Arbeitsbekleidung.Models.Models;assembly=Arbeitsbekleidung.Models"
    d:DataContext="{d:DesignInstance Type=viewmodels:BestandAnzeigenViewModel}"
    d:DesignHeight="800"
    d:DesignWidth="1250"
    d:Background="White"
    mc:Ignorable="d">
   
    <Grid>        
        <ListView ItemsSource="{Binding Arbeitskleidungen}" Grid.Row="0">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:Arbeitskleidung}">
                    
                    <Grid Margin="0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0">
                                <Run FontWeight="Bold" Text="Artikelname: " />
                                <Run Text="{Binding DisplayArtikelName, Mode=OneWay}" />
                            </TextBlock>

                            <TextBlock Grid.Column="1" Margin="5,0">
                                <Run FontWeight="Bold" Text="Größen: " />
                                <Run Text="{Binding groesse}" />
                            </TextBlock>

                            <TextBlock Grid.Column="2" Margin="5,0">
                                <Run FontWeight="Bold" Text="Gesamtbestand: " />
                                <Run Text="{Binding GesamtBestand, Mode=OneWay}" />
                            </TextBlock>
                        </Grid>
                        
                        <ListView
                            Grid.Row="1"
                            Margin="5,0,0,0"
                            BorderThickness="0"
                            ItemsSource="{Binding Bestände}">
                            <ListView.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:Bestand}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0">
                                            <Run FontWeight="Medium" Text="Lagerplatz: " />
                                            <Run Text="{Binding Lagerplatz.lpz_bezeichnung}" />
                                        </TextBlock>

                                        <TextBlock Grid.Column="1" Margin="10,0">
                                            <Run FontWeight="Medium" Text="Menge: " />
                                            <Run Text="{Binding menge}" />
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>                        
                    </Grid>
                    
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <DataGrid
            AutoGenerateColumns="False"
            Background="White"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeRows="False"
            FontSize="18"
            IsReadOnly="True"
            ItemsSource="{Binding Arbeitskleidungen}"
            VerticalScrollBarVisibility="Auto">
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding GesamtBestand, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="0">
                            <Setter Property="Background" Value="IndianRed" />
                            <Setter Property="Foreground" Value="White" />
                        </DataTrigger>

                        <DataTrigger Binding="{Binding IsMeldegrenzeÜberschritten, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="Background" Value="DarkOrange" />
                            <Setter Property="Foreground" Value="White" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    MinWidth="120"
                    Binding="{Binding DisplayArtikelName, Mode=OneWay}"
                    Header="Artikelname" />
                <DataGridTextColumn
                    Width="Auto"
                    MinWidth="120"
                    Binding="{Binding groesse}"
                    Header="Größe" />
                <DataGridTextColumn
                    Width="Auto"
                    MinWidth="120"
                    Binding="{Binding GesamtBestand, Mode=OneWay}"
                    Header="Gesamtbestand" />
            </DataGrid.Columns>
            
            <DataGrid.RowDetailsTemplate>
                <DataTemplate DataType="{x:Type models:Arbeitskleidung}">
                    <ListView
                        Grid.Row="1"
                        Margin="5,0,0,0"
                        BorderThickness="0"
                        ItemsSource="{Binding Bestände}">
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:Bestand}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0">
                                        <Run FontWeight="Medium" Text="Lagerplatz: " />
                                        <Run Text="{Binding Lagerplatz.lpz_bezeichnung}" />
                                    </TextBlock>

                                    <TextBlock Grid.Column="1" Margin="10,0">
                                        <Run FontWeight="Medium" Text="Menge: " />
                                        <Run Text="{Binding menge}" />
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
    </Grid>
</UserControl>